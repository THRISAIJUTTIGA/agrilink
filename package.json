{
  "name": "agrilink",
  "version": "1.0.0",
  "private": true,
  "description": "AgriLink — Smart Farmer Market & Buyer Platform (prototype)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "npx concurrently -k -n SERVER,CLIENT -c green,cyan \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
